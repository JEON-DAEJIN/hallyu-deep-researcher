# N043 실습계획 — 한류 콘텐츠 산업의 확산 딥리서치 에이전트

2026-09-23 작성

## 0. 이 문서의 목적

N043 과제는 "완성된 코드를 따라 치는" 수업이 아니라 **N042의 코디네이터+조사관+편집자 6노드 구조를 나만의 주제로 처음부터 다시 설계해서 제출**하는 프로젝트다. 이 문서는 그 설계·구현·검증·제출까지의 전체 계획을 단계별로 정리한다. 참고: [N043 강의노트](../강의정리/N043/N043_나만의딥리서치에이전트(deep-research-project).md), [N042 실습 결과보고서](../강의정리/N042/N042_실습결과보고서_2026-09-23.md)(코드 구조 오류·데이터 오염 두 가지 실전 교훈이 담긴 문서).

## 1. 주제 선정 — "한류 콘텐츠 산업의 확산"

**좋은 주제의 3조건 대조**

| 조건 | 충족 근거 |
|---|---|
| ① 여러 자료가 필요함 | 아티스트·기획사·작품·정책 문서가 각각 따로 존재 — 하나로는 답 안 나옴 |
| ② 자료 총량이 컨텍스트 창을 넘음 | 문서 30~60건 × 문서당 수천~수만 자 → gpt-4o-mini 한 번에 다 못 읽음 |
| ③ 문서끼리 연결 관계가 있음 | 방탄소년단→하이브→SM/JYP/YG(경쟁사), 봉준호→기생충→아카데미 등 다단계 링크 존재 |

**분업 단위(내용 단위, 형식 단위 아님)**: 음악(K-pop) 확산 / 영화·드라마 / 산업구조(기획사·플랫폼) / 위기와 대응(한한령 등) — 각 절이 서로 다른 문서 군집을 가리키도록 설계했다(N042 부록B "형식 단위로 나누면 전원이 같은 문서를 읽는다" 함정 회피).

## 2. 전체 실행 단계

| 단계 | 산출물 | 상태 |
|---|---|---|
| ① 저장소 스캐폴딩 | `config.json`, `data/questions.json`, `collect_corpus.py`, `.gitignore`, `.env.example`, `requirements.txt`, 이 `PLAN.md` | **완료 (이번 커밋)** |
| ② GitHub 저장소 생성 + 초기 커밋 | `JEON-DAEJIN/hallyu-deep-researcher` | **완료 (이번 커밋)** |
| ③ 코퍼스 실제 수집 | `data/corpus.json` (30건 이상, 3,000자 미만 제외) | 다음 단계 |
| ③-1 코퍼스 사람 검수 | 문서 제목 목록 훑어보고 무관 문서 배제 (N042 5장 교훈) | 다음 단계 |
| ④ `graph.py` 구현 | plan→dispatch→researcher→review→synthesize, N042 패턴 재사용 | 다음 단계 |
| ⑤ `metrics.py` 구현 | 근거율·허위인용·편중·읽고안쓴문서 4개 지표 | 다음 단계 |
| ⑥ `baseline.py` 구현 | 팀과 동일 자료·모델·예산의 단일 에이전트 대조군 | 다음 단계 |
| ⑦ `ablation.py` 구현 | 배정→역할→재위임→구역 순서로 한 번에 하나씩 끄는 실험 | 다음 단계 |
| ⑧ 실제 실행 & 반복 | 질문 9건 × 기본 파이프라인, 대표 질문 재현성 확인(3회 반복) | 다음 단계 |
| ⑨ `app.py` 웹 데모 | streamlit, 절마다 누가 무엇을 읽고 썼는지 시각화, 로컬 실행 캡처 | 다음 단계 |
| ⑩ `REPORT.md` 작성 | 6개 항목(주제/코퍼스, 분업설계, 지표, 구조도, 데모, 회고) | 다음 단계 |
| ⑪ 루브릭 자가 점검 + 최종 커밋/푸시 | — | 다음 단계 |

## 3. 코퍼스 설계

- 시드 20건: 한류·케이팝·아티스트(방탄소년단·블랙핑크·아이유·싸이)·기획사(하이브·SM·JYP·YG)·영화/드라마(기생충·봉준호·오징어 게임·겨울연가·대장금)·플랫폼(넷플릭스)·정책/사건(한한령·사드 배치 논란)·강남스타일·빌보드 차트
- 2홉 확장으로 최대 60건까지, 3,000자 미만 토막글 제외
- N042 데이터 오염 교훈을 반영해 **수집 스크립트 자체에 스톱리스트를 미리 내장**: 국민 허브(네이버·구글·유튜브 등), 날짜/목록/분류 문서, 국가·일반개념 허브(대한민국·미국·영화·음악 등)
- 수집 직후 문서 제목 목록을 사람이 직접 훑어 추가 오염 여부 확인(자동 필터링만 믿지 않는다)

## 4. 질문 설계 (`data/questions.json`, 9건)

단순 2건(질문 1, 7 — 이 구조가 불필요한 경우를 보여주기 위해 의도적으로 포함) + 복합 4건 + 추적 3건. 질문마다 "왜 나눌 만한가"를 한 줄로 기록.

## 5. 코디네이터·조사관·편집자 설계 — N042에서 무엇을 그대로 쓰고 무엇을 바꾸는가

| 항목 | N042 | N043(이 프로젝트) |
|---|---|---|
| 목차 분할 기준 | 내용 단위(발생배경/주요사건/핵심인물/결과영향) | 내용 단위(음악/영화드라마/산업구조/위기대응) — 동일 원칙, 다른 도메인 |
| 재위임 시 원고 처리 | 무조건 최신본으로 덮어씀(기본값) | **직접 정책을 설계한다**: 재위임 후 인용 수가 이전보다 줄어들면 이전 원고를 유지하고, 늘어나면 교체한다 — 그 이유를 REPORT.md에 기록 |
| 배정 검증 | 코드로 존재 문서인지 확인 | 동일 원칙 유지 + 검증을 잠시 꺼서 실제로 지어낸 문서명이 배정되는지 재현(도구설계원칙 6) |
| dispatch 함수 등록 방식 | 노드 목록에서 제외, 조건부 엣지로만 등록 | **동일하게 처리** — N042에서 겪은 "노드/라우터 겸용 등록" 버그를 처음부터 회피 |

## 6. 지표 설계 — 무엇이 어느 장치를 보는 신호인가

| 지표 | 관련 장치 | 정상 범위 |
|---|---|---|
| 근거율 | 조사·재위임 | 높을수록 좋음(순위표 아님, 명백한 실패만 거름) |
| 허위인용 | 조사/인용 검증 | 0이어야 하는 경보 |
| 편중 | 배정·구역 | 낮을수록 좋음 |
| 읽고 안 쓴 문서 | 배정 | 낮을수록 조사 효율 높음 |

오귀속(읽은 문서를 엉뚱한 문장에 붙이는 것)은 위 4개 지표로 잡히지 않는다는 점을 REPORT.md에 명시하고, 대표 질문 1~2건은 사람이 직접 읽어 확인한다.

## 7. 대조군(`baseline.py`) — 공정성 체크리스트

- [ ] 팀과 동일한 코퍼스(`data/corpus.json`) 사용
- [ ] 팀과 동일한 모델(gpt-4o-mini) 사용
- [ ] 팀의 절수 × 절당예산과 같은 총 읽기 예산(4절×4문서=16건)을 단일 에이전트에도 부여
- [ ] 예산을 실제로 다 쓰는지 로그로 확인(중간에 멈추면 공정하지 않음)

## 8. Ablation 설계 — 한 번에 하나씩만

순서: 기본 → 배정 끄기(랜덤 배정) → 역할 끄기(모든 절 동일 역할) → 재위임 끄기(바퀴=0 고정) → 구역 끄기(피하기 리스트 비움). 대표 질문(복합 1건 + 추적 1건)으로 각 설정을 3회 반복 실행해 변동폭을 확인한 뒤 결론을 내린다(N042 12장 "한 번의 결과를 믿지 않는다" 원칙).

## 9. 웹 데모(`app.py`)

streamlit으로 질문 입력 → 진행 로그(어느 절을 누가 담당, 무슨 문서를 읽었는지) → 최종 보고서 → 지표 요약까지 한 화면에 표시. 숫자만 보여주는 화면은 요점을 놓친다는 14장 지침을 반영.

## 10. 제출 루브릭 자가 점검표

| 평가기준 | 확인 방법 |
|---|---|
| 성능 검증 및 실패 분석 | 기본/장치끔/대조군 보고서를 나란히 읽고 REPORT.md 회고에 직접 비교 서술 |
| 분업 설계와 컨텍스트 격리의 타당성 | 목차 내용 단위 분할 + 컨텍스트 격리율 실측값 REPORT.md에 기록 |
| 엔드투엔드 구동 및 완결성 | `collect_corpus.py`→`graph.py`→`app.py`까지 오류 없이 로컬 실행, 캡처 첨부 |

## 11. DJ의 도구 설계 체크리스트 반영 지점

`config.json`의 `도구설계원칙` 7개 항목이 이 프로젝트의 어느 코드에 대응하는지는 위 표들과 `config.json` 주석에 명시했다. REPORT.md 회고에서 "이 체크리스트를 실제로 적용해보니 무엇이 달라졌는가"를 별도로 기록한다.

## 12. 리스크 / N042에서 이미 겪은 함정 재발 방지

- dispatch를 노드로 이중 등록하지 않는다(4절 표 참고)
- 코퍼스 수집 직후 반드시 사람이 제목 목록을 눈으로 검수한다(3절 참고)
- ablation에서 두 개 이상의 장치를 동시에 끄지 않는다(8절 참고)
- 재위임 원고를 무조건 덮어쓰지 않는다 — 정책을 정하고 이유를 남긴다(5절 참고)
- 한 번의 실행 결과로 결론 내리지 않는다 — 대표 질문은 3회 반복(8절 참고)
